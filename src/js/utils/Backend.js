'use strict';

import {ActionTypes} from '../Constants';
import Actions from '../actions/Actions';

class Backend {
  getMovies(callback, options) {
  }
}

let backend = new Backend();
export default backend;
